#! /bin/bash

# Declare all variables first
PROJECT_ROOT=""
PYTHON_ROOT=""

# Now assign values to the variables
PROJECT_ROOT="${PWD}"
PYTHON_ROOT=${PROJECT_ROOT}/glyph-catcher

PACKAGE_NAME="$(basename "${PWD}")"
# Export all variables
export PROJECT_ROOT
export PYTHON_ROOT
export PACKAGE_NAME
